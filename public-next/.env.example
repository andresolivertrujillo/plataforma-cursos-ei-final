# En Vercel, usa la URL pública del backend desplegado en Render.
# La barra final es opcional; la aplicación la elimina si está presente.
# En producción, la aplicación rechazará una configuración ausente o vacía.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
